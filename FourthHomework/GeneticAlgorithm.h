#pragma once
#include"GoalFunction.h"
#include<bitset>

std::vector<double> GeneticAlgorithm() {
}


std::vector<std::string> createPopulation(int precision, int noOfBits, int populationSize, int dimension) {

	std::vector<std::vector<std::string>> initialPopulation;

	for (int i = 0; i < populationSize; i++) {

		std::vector<double> point;
		for (int j = 0; j < dimension; j++) {

		}
	}

}

